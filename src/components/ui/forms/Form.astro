---
// Form
// ------------
// Description: The form component
// Properties:
// - method: The method of the form (post, get, dialog)
// - classes: Additional classes to be added to the form

// Props
type Props = {
	method?: 'post' | 'get' | 'dialog'
	action?: string
	id?: string
	classes?: string
}
const { method = 'post', action, id, classes } = Astro.props
---

<form method={method} action={action} id={id} novalidate class:list={[{ [`${classes}`]: classes }]}>
	<slot />
</form>
